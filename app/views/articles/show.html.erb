<div class="large-container center-block">
  <% if @article %>
    <h1><%= @article.title %></h1>
    <div>
      <p>Written by: <strong><%= @article.user.email %></strong></p>
      <% if @article.user.avatar.attached? %>
        <%= image_tag url_for(@article.user.avatar), height:30 %>
      <% end %>
    </div>
    <div>
      <%= @article.content %>
    </div>
  <% else %>
    <p> Article not found</p>
  <% end %>

  <div class="margin-top">
    <p class="label">Tags</p>
    <% @article.categories.each do |category| %>
      <span class="badge badge-pill padding" style="background-color: <%= category.color %>">
        <%= link_to category.title, category, class:"text-white" %>
      </span>
    <% end %>
  </div>

  <% if @article.user == current_user %>
    <div>
      <%= link_to 'Delete article', @article, method: :delete, data: { confirm: '¿are you sure? This action can not be undone'} %><br/>
    </div>
  <% end %>
</div>
